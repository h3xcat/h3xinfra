# Managed by h3xinfra 00-prep. Restart K3s during coordinated maintenance.
# override_rc leaves deferred services visible and permits manual restarts.
# Extend the distribution defaults instead of replacing the override map.
$nrconf{override_rc}->{qr(^k3s(-agent)?\.service$)} = 0;
# Match both cri-containerd scopes and service names reported by needrestart.
$nrconf{override_rc}->{qr(^cri-containerd-)} = 0;
