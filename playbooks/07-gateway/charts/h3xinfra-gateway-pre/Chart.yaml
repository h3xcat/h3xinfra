apiVersion: v2
name: h3xinfra-gateway-pre
description: Pre-flight resources for the Envoy Gateway-managed shared Gateway (wildcard certificate, MetalLB IP pool, GatewayClass, Gateway, and ReferenceGrants for cross-namespace cert/backend access).
type: application
version: 0.1.0
appVersion: "1.4.2"
