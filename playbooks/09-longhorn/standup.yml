---
- import_playbook: ../99-utils/kube-connect.yml

- name: Setup Longhorn CLI and check preflight requirements
  hosts: k3s_cluster
  gather_facts: true
  become: true
  vars:
    longhorn_cli_version: "{{ cli_versions.longhorn_cli }}"
    longhorn_cli_arch: "{{ 'amd64' if ansible_architecture == 'x86_64' else 'arm64' }}"
  serial: 1 
  tasks:
    - name: Download Longhorn CLI
      get_url:
        url: "https://github.com/longhorn/cli/releases/download/{{ longhorn_cli_version }}/longhornctl-linux-{{ longhorn_cli_arch }}"
        dest: "/usr/local/bin/longhornctl"
        mode: '0755'
        owner: root
        group: root

    - name: Verify Longhorn CLI installation
      command: longhornctl version
      register: longhorn_cli_version_output
      changed_when: false

    - name: Display Longhorn CLI version
      debug:
        msg: "{{ longhorn_cli_version_output.stdout }}"

# V2 (SPDK) data engine prerequisites. Only the amd64 servers run V2:
# the Pis are excluded (hugepage + dedicated-CPU cost on 4-core/16GB
# nodes, and arm64+NVMe stuck-I/O bug longhorn/longhorn#13243), so they
# need no hugepages or nvme-tcp.
- name: Prepare amd64 nodes for the Longhorn V2 data engine
  hosts: k3s_cluster
  gather_facts: true
  become: true
  tasks:
    - name: Configure V2 data engine prerequisites (amd64 only)
      when: ansible_architecture == 'x86_64'
      block:
        - name: Reserve 2MiB hugepages for SPDK (2 GiB total)
          ansible.posix.sysctl:
            name: vm.nr_hugepages
            value: "1024"
            sysctl_file: /etc/sysctl.d/99-longhorn-v2.conf
            reload: true

        - name: Verify hugepages were actually allocated
          ansible.builtin.command: grep HugePages_Total /proc/meminfo
          register: hugepages_total
          changed_when: false
          failed_when: (hugepages_total.stdout.split()[1] | int) < 1024

        - name: Load nvme-tcp kernel module at boot
          ansible.builtin.copy:
            dest: /etc/modules-load.d/longhorn-v2.conf
            content: "nvme_tcp\n"
            mode: "0644"

        - name: Load nvme-tcp kernel module now
          ansible.builtin.command: modprobe nvme_tcp
          changed_when: false

- name: Deploy Longhorn via Helm
  hosts: k8s
  gather_facts: true
  vars:
  tasks:
    - name: Get cluster nodes for V2 engine node selection
      kubernetes.core.k8s_info:
        kind: Node
      register: cluster_nodes

    # Must happen before the Helm deploy enables v2DataEngine, so the
    # Pis never attempt to start a V2 instance manager (which would
    # crashloop without hugepages).
    - name: Exclude arm64 nodes from the V2 data engine
      kubernetes.core.k8s:
        state: patched
        kind: Node
        name: "{{ item.metadata.name }}"
        definition:
          metadata:
            labels:
              node.longhorn.io/disable-v2-data-engine: "true"
      loop: "{{ cluster_nodes.resources }}"
      loop_control:
        label: "{{ item.metadata.name }}"
      when: item.metadata.labels['kubernetes.io/arch'] == 'arm64'

    - name: Add Longhorn Helm repository
      kubernetes.core.helm_repository:
        name: longhorn
        repo_url: https://charts.longhorn.io
        state: present
        force_update: true
      changed_when: false

    - name: Create Longhorn namespace
      kubernetes.core.k8s:
        api_version: v1
        kind: Namespace
        name: "{{ longhorn.namespace }}"
        state: present
        wait: true

    - name: Deploy Longhorn pre-configuration via Helm
      kubernetes.core.helm:
        name: "{{ longhorn.release_name_prefix }}-pre"
        chart_ref: "{{ playbook_dir }}/charts/h3xinfra-longhorn-pre"
        namespace: "{{ longhorn.namespace }}"
        create_namespace: false
        values:
          fullnameOverride: "{{ longhorn.release_name_prefix }}-pre"
          longhornCifsSecret:
            cifsUsername: "{{ longhorn.cifs_username }}"
            cifsPassword: "{{ longhorn.cifs_password }}"
        state: present
        wait: true
        timeout: "120s"
      no_log: true

    - name: Deploy Longhorn via Helm
      kubernetes.core.helm:
        name: "{{ longhorn.release_name_prefix }}-main"
        chart_ref: "longhorn/longhorn"
        chart_version: "{{ chart_versions.longhorn }}"
        namespace: "{{ longhorn.namespace }}"
        create_namespace: false
        values: 
          defaultSettings:
            # Storage Configuration
            defaultDataPath: /var/lib/longhorn/
            storageMinimalAvailablePercentage: 10
            storageOverProvisioningPercentage: 200
            storageReservedPercentageForDefaultDisk: 5

            # Performance Optimization
            # 3 replicas tolerates one node loss during rebuild — needed
            # because the cluster has 8 worker-capable nodes and the
            # default-2 setting was inconsistent with the actual fleet
            # state (every volume was manually bumped to 3 over time).
            defaultReplicaCount: 3
            defaultDataLocality: best-effort
            replicaAutoBalance: least-effort

            # High Availability and Resilience
            replicaSoftAntiAffinity: "false"
            allowVolumeCreationWithDegradedAvailability: "false"
            rwxVolumeFastFailover: "true"
            # Auto-failover stuck pods (StatefulSet/Deployment) when their node goes down,
            # so RWO volumes can be reattached on a healthy node without manual intervention.
            nodeDownPodDeletionPolicy: delete-both-statefulset-and-deployment-pod
            # Drain compatibility for kured + manual kubectl drain.
            #
            # `block-for-eviction-if-contains-last-replica`: when a node is
            # cordoned for drain, if it holds the last healthy replica of any
            # volume, Longhorn ACTIVELY evicts that replica (rebuilds it on
            # another node) and only then lifts the instance-manager PDB so the
            # drain can complete. Replicas that already have healthy copies
            # elsewhere are not rebuilt — only the at-risk ones move.
            #
            # We previously used `block-if-contains-last-replica`, which only
            # *blocks* and never relocates. That deadlocked kured: during a
            # back-to-back rolling reboot of several storage nodes, replicas on
            # the just-rebooted nodes were still rebuilding, so the next node to
            # drain genuinely held the last healthy replica of some volume. The
            # instance-manager PDB then refused eviction indefinitely and kured
            # ground on the un-evictable pod until it crashed mid-drain, leaving
            # the node cordoned forever and the volume stuck faulted.
            nodeDrainPolicy: block-for-eviction-if-contains-last-replica

            # Instance Manager CPU (V1 Data Engine)
            guaranteedInstanceManagerCpu: 15

            # V2 Data Engine (SPDK), GA since 1.12. Enabled cluster-wide,
            # but arm64 nodes are excluded via the
            # node.longhorn.io/disable-v2-data-engine label applied above.
            # V2 replicas additionally require block-type disks, which are
            # registered per-node (none yet — carve an LV from ubuntu-vg
            # and add it as a block disk on the Longhorn node CR).
            v2DataEngine: "true"

            # Backup and Maintenance
            createDefaultDiskLabeledNodes: "false"
          defaultBackupStore:
            backupTarget: "{{ longhorn.cifs_backup_target }}"
            backupTargetCredentialSecret: "{{ longhorn.release_name_prefix }}-pre-cifs-secret"

        state: present
        wait: true
        # A version bump rolls the longhorn-manager DaemonSet (image pulls +
        # one restart per node), which routinely overruns 120s on this fleet.
        timeout: "600s"

    - name: Validate Longhorn OIDC configuration
      ansible.builtin.assert:
        that:
          - longhorn.auth.oauth2_proxy.oidc_issuer_url | default('') | length > 0
          - longhorn.auth.oauth2_proxy.client_id     | default('') | length > 0
          - longhorn.auth.oauth2_proxy.client_secret | default('') | length > 0
        fail_msg: >-
          longhorn.auth.method == 'oauth2_proxy' requires
          longhorn.auth.oauth2_proxy.{oidc_issuer_url,client_id,client_secret}
          to all be set.
      when: (longhorn.auth.method | default('none')) == 'oauth2_proxy'

    - name: Deploy Longhorn configuration via Helm
      kubernetes.core.helm:
        name: "{{ longhorn.release_name_prefix }}-post"
        chart_ref: "{{ playbook_dir }}/charts/h3xinfra-longhorn-post"
        namespace: "{{ longhorn.namespace }}"
        create_namespace: false
        values:
          fullnameOverride: "{{ longhorn.release_name_prefix }}-post"
          longhornIngress:
            host: "{{ longhorn.ingress_host }}"
            gateway:
              name: "{{ gateway.release_name_prefix }}-pre-shared"
              namespace: "{{ gateway.namespace }}"
            trustedIPs: "{{ longhorn.trusted_ips }}"
            oidc:
              enabled: "{{ (longhorn.auth.method | default('none')) == 'oauth2_proxy' }}"
              issuerUrl: "{{ longhorn.auth.oauth2_proxy.oidc_issuer_url | default('') }}"
              clientId: "{{ longhorn.auth.oauth2_proxy.client_id | default('') }}"
              clientSecret: "{{ longhorn.auth.oauth2_proxy.client_secret | default('') }}"
              secretRef:
                name: "{{ longhorn.release_name_prefix }}-oidc"
          recurringJobs:
            enabled: "{{ longhorn.recurring_jobs.enabled | default(false) }}"
        set_values:
          # `jobs` is a list of dicts; pass through to_json so Helm gets
          # native YAML/JSON instead of an Ansible-stringified list.
          - value: "recurringJobs.jobs={{ longhorn.recurring_jobs.jobs | default([]) | to_json }}"
            value_type: json
        state: present
        wait: true
        timeout: "120s"
      no_log: true
